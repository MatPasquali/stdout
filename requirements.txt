# Collection
requests>=2.31
feedparser>=6.0

# Local env loading (.env). In CI the key comes from secrets, so this is optional.
python-dotenv>=1.0

# Note: the Gemini writer calls the REST API directly via `requests`, so no
# extra LLM SDK is needed. The whole stack stays light.
